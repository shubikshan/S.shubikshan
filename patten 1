#include <iostream>
int main()
{
	int n,a,k;
	scanf("%d",&n);
	for (k=1; k<=n; k=k+1)
	{
		for (a=1; a<=n; a=a+1)
			printf(" ");
		for(a=1; a<=2*k-1; a=a+1)
			printf("*");
		printf("\n");
	}
	return 0;
}
