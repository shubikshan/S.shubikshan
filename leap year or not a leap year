#include <iostream>

int main()
{
    int n;
    printf("enter the year");
    scanf("%d",&n);
    if(n%4==0)
    {
        if(n%400==0)
        printf("not a leap year" );
        else 
        printf("leap year");
    }
    else
    {
        printf ("not a leap year");
    }
    return 0;
}
